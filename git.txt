ssh-keygen -t rsa -C "vpsxt@outlook.com"

git config --global user.name "vpsxt"
git config --global user.email "vpsxt@outlook.com"

git config --global user.name "mibapw"
git config --global user.email "mibapw@outlook.com"

git config --global user.name "acspw"
git config --global user.email "acspw@outlook.com"

git init
git add .
git commit -m "20170330"
git push -u origin master